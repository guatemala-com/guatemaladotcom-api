// Re-export from the new scopes file for backward compatibility
export {
  OAuthAuth,
  OAuthAuthRead,
  OAuthAuthWrite,
  OAuthAuthAdmin,
  OAuthAuthScopes,
  OAUTH_AUTH_KEY,
  OAUTH_SCOPE_KEY,
} from './oauth-scopes.decorator';
